[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = []

# Serve the canonical Privacy Notice from the privacy.propertykickstart.co.uk domain alias.
# The privacy domain must be assigned to this Netlify project before DNS is switched live.
[[redirects]]
  from = "https://privacy.propertykickstart.co.uk/*"
  to = "/property-kickstart-privacy-v1.0-2026-08-19.html"
  status = 200
  force = true

# Redirect API calls to the Netlify function
[[redirects]]
  from = "/api/epc-lookup"
  to   = "/.netlify/functions/epc-lookup"
  status = 200

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"
    Content-Security-Policy = """
      default-src 'self';
      script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com;
      style-src 'self' 'unsafe-inline';
      connect-src 'self' https://www.google-analytics.com https://region1.google-analytics.com;
      img-src 'self' data: https:;
      font-src 'self';
      frame-ancestors 'none';
    """
